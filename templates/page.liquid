<div class="container page-content">
  <h1>{{ page.title }}</h1>
  <div class="page-body">
    {{ page.content }}
  </div>
</div>

<style>
  .page-content {
    padding: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }
  
  .page-body {
    margin-top: 1.5rem;
  }
</style>
